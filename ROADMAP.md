# ProudMe · 待办与已知约束

这个文件记录**当前做不了、要等条件成熟才能做**的事，以及做的时候需要注意什么。
不记录已经完成的功能 —— 那些看 git log。

---

## 一、要等做成原生 App 才能做的

网页（PWA）有几件事在原理上做不到，不是实现问题。封装成原生 App
（Capacitor / React Native）之后就可以。

### 1. 读取手机自带日历，自动生成当天目标

**现状**：接入点已经预留在 `index.html` 里，搜 `系统日历接入` 就能找到。

链路已经写好了，只差一个函数：

```
readDeviceCalendarToday()  →  [{title, calendarName}]
      ↓ guessCategory()        按关键词归到 学习/运动/社交/休闲
applyExternalGoals()       →  填进当天空着的目标格子
```

**封装后要做的**：

1. 装日历插件，例如 `@ebarooni/capacitor-calendar`
2. 请求日历读取权限
3. 取当天 00:00–24:00 的事件，映射成 `{title, calendarName}`
4. 挂到 `window.ProudMeNative.readCalendarToday`，`readDeviceCalendarToday()`
   会自动调用它

iOS 还需要在 `Info.plist` 里加 `NSCalendarsUsageDescription`，写清楚为什么读日历。

**在那之前**：目标每天清空后，靠首页的「↩︎ 沿用昨天的目标」恢复。

### 2. 可靠的每日提醒

**现状**：网页版的提醒必须 App 开着（哪怕在后台）才会响；没开着的话，
用户下次打开会看到首页的开场提示。界面里已经如实说明了这一点。

**封装后**：用本地通知（Local Notifications），关掉 App 也能按时提醒。
这是上架最直接的收益之一。

---

## 二、要等有后端才能做的

### 3. 好友排行榜

**现状**：排行榜页面已经做好，但只显示自己真实的战绩，**没有放任何假数据**。
比拼靠「生成今日战绩卡」发到群里。

**要做的**：邀请码建房间、每天自动排名、前三名发积分。需要账号系统。
推荐 Supabase 免费套餐（anon key 设计上可以公开放前端，配合 RLS 权限规则）。

**未成年人数据，必须按最小化原则做**：

- 只存昵称，不存真名，不上传头像照片（宠物照片留在本地）
- 只存打卡数和日期，**不上传**目标内容、心情、悄悄话 —— 那些永远只在本机
- 房间靠邀请码进，不搜索、不推荐，陌生人加不进来
- 随时可退出并删除自己在服务器上的全部记录

**两条防「公开处刑」的设计**（已经商定，实现时别忘）：

- 排行榜按**打卡天数**排，不按专注时长排 —— 比谁更坚持，不是比谁学得更久，
  后者会直接演变成卷时长
- 只显示**前三名 + 自己的位置**，不公开完整排名，垫底的人不会被示众

---

## 三、上架 Apple Store 之前必须处理的

### 4. 移除 Google Analytics

`index.html` 头部现在有 `gtag.js`。**Apple 儿童类目（Kids Category）
禁止第三方分析工具**。如果目标用户是青少年、走儿童类目，这个必须拿掉。

即使不走儿童类目，只要面向未成年人，App Privacy 隐私标签也要如实申报
数据收集情况。

### 5. 纯网页套壳会被拒

审核指南 4.2（最低功能要求）会拒绝只是把网页包起来的 App。
上架需要真正接入原生能力 —— 上面第 1、2 条（日历、本地通知）正好是
最自然的两个，做了它们也就同时解决了这个问题。

---

## 四、已经商定但还没做的小事

- 段位达成（黄金 10 天 / 钻石 21 天 / 王者 30 天 / 大圣 60 天）时的庆祝
  目前只有一个小提示。10 天坚持下来，值得一个更大的时刻。

---

## 五、开发时的注意事项

- **Service Worker 会缓存旧版本**。每次发版后如果看到的还是旧样子，
  删掉主屏图标重装一次。改动较大时记得把 `sw.js` 里的 `CACHE` 版本号 +1。
- **所有数据只存在用户自己的手机浏览器里**。清缓存、换手机、无痕模式都会
  让记录归零。「我的」里有导出/导入备份，界面里也如实提示了。
- **GitHub Pages 改仓库名不会跳转**。仓库页面会跳，但
  `github.io/旧名/` 是 404。已经从 `habit-quest` 改成 `ProudMe`，
  旧网址已失效。
